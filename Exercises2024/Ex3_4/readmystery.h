#ifndef READMYSTERY_H
#define READMYSTERY_H

#include <string>
#include <vector>

// Function to read the first N numbers from a file and return them as a vector
std::vector<double> readMystery(const std::string& filePath, int N);

#endif
